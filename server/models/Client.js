const mongoose = require("mongoose");

// Create Mongoose schema for Client Table/Model/Collection in our MongoDB

const ClientSchema = new mongoose.Schema({
  name: {
    type: String,
  },
  email: {
    type: String,
  },
  phone: {
    type: String,
  },
});

module.exports = mongoose.model("Client", ClientSchema);
